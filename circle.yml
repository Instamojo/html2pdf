machine:
  services:
    - docker

dependencies:
  override:
    - pip install requests
    - docker build -t html2pdf:latest .
    - docker run -it -p 12345:80 html2pdf

test:
  override:
    - python tests.py